# Удаленный сервер

## База данных

Вход в командную строку SQL:
```shell script
mysql -u root -p
```

Дамп:  
```shell script
mysqldump -u root -p expenses > dump.sql
```

### Командная строка
";" в конце обязательна!
  
Список баз: `show databases;`  
Выход: `exit;`